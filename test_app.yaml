service: test-app
runtime: python
env: flex
entrypoint: gunicorn -b :$PORT mozio.wsgi --timeout 500

runtime_config:
  python_version: 3.7

handlers:
  - url: /static
    static_dir: static/

  - url: /.*
    script: auto

env_variables:
  DB_NAME: "postgres_test"
  DB_USERNAME: "postgrestest"
  DB_PASSWORD: "Pakistan@1496"
  DB_HOST: "/cloudsql/celestial-gecko-271013:us-central1:virya-dev"

beta_settings:
  cloud_sql_instances: "celestial-gecko-271013:us-central1:virya-dev"

manual_scaling:
  instances: 1

resources:
  cpu: 2
  memory_gb: 4
  disk_size_gb: 10
